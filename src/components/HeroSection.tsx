import React from 'react';

const HeroSection: React.FC = () => {
  return (
    <div className="hero-section">
         <img src="HeroSection.jpg" alt="Logo" style={{height: '100%', width: "100%"}} />
  </div>
  );
};

export default HeroSection;
